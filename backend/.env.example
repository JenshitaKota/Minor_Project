PORT=4000
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/pharma_integrity?schema=public"
JWT_SECRET=change_me

RPC_URL=http://127.0.0.1:8545
CONTRACT_ADDRESS=
# Two independent attestors are required to anchor anything (propose + co-sign) - see
# AnchorRegistry.sol. In production these should be held and operated by genuinely
# separate systems, not both configured on one backend as this local dev setup does.
QA_ATTESTOR_PRIVATE_KEY=
AUDITOR_ATTESTOR_PRIVATE_KEY=
